<script type="text/ng-template" id="sign_up_in.html">
  <div class="modal-header">
    <h3>Sign up / in</h3>
  </div>
  <form name="form" validate>
    <div class="modal-body">
      <div class="form-horizontal" role="form">
        <div class="form-group">
          <label for="email" class="control-label col-sm-2">Email</label>
          <div class="col-sm-8">
            <input type="email" class="form-control" id="email" ng-model="user.email" name="userEmail" focus-me required />
          </div>
          <span ng-show="form.userEmail.$dirty && form.userEmail.$invalid" class="validation-error">
            <span ng-show="form.userEmail.$error.required">Tell us your email.</span>
            <span ng-show="form.userEmail.$error.email">This is not a valid email.</span>
          </span>
        </div>
        <div class="form-group">
          <label for="password" class="control-label col-sm-2">Password</label>
          <div class="col-sm-8">
            <input type="password" class="form-control" id="password" name="userPassword" ng-model="user.password" required />
          </div>
          <span ng-show="form.userPassword.$dirty && form.userPassword.$invalid" class="validation-error">
            <span ng-show="form.userPassword.$error.required">Write your password.</span>
          </span>
        </div>
      </div>
      <div class="reset-password">
        <a href="#">Click here to reset your password</a>
      </div>
      <div class="sign-up-explanation">
        If the username exists, we will try to sign you in.<br />
        If the username does not already exist, a new user will be made and you will be signed in.<br />
        All users that have not been active for 30 days will be destroyed routinely.
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn btn-success"
              ng-click="signUpIn()"
              ng-disabled="form.$invalid">Sign up / In</button>
      <button class="btn btn-link" ng-click="cancel()">Cancel</button>
    </div>
  </form>
</script>
